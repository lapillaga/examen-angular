import { UniqueIdentificacionDirective } from './unique-identificacion.directive';

describe('UniqueIdentificacionDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueIdentificacionDirective();
    expect(directive).toBeTruthy();
  });
});
